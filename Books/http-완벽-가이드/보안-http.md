# HTTPS와 네트워크 학습 정리

## 1. 대칭키 vs 비대칭키 암호화

### 대칭키 (Symmetric Key)

- **키 개수**: 1개
- **특징**: 암호화 키 = 복호화 키
- **장점**: 빠른 속도 🚀
- **단점**: 키 공유가 어려움 (키 분배 문제)
- **다른 이름**: 세션키, 비밀키, 공유키 (모두 같은 개념!)
- **사용 예시**: AES-256

### 비대칭키 (Asymmetric Key)

- **키 개수**: 2개 (페어)
  - 공개키 (Public Key): 누구나 볼 수 있음
  - 개인키 (Private Key): 소유자만 가짐
- **특징**: 공개키로 암호화 → 개인키로만 복호화
- **장점**: 안전한 키 교환 가능
- **단점**: 느린 속도 🐢
- **사용 예시**: RSA-2048

### 보안 강도 비교

| 방식                | 보안 강도        | 속도    |
| ------------------- | ---------------- | ------- |
| AES-256 (대칭키)    | 매우 강함 🔐🔐🔐 | 빠름 🚀 |
| RSA-2048 (비대칭키) | 매우 강함 🔐🔐🔐 | 느림 🐢 |

**중요**: 대칭키가 "최소한의 암호화"가 아님! 둘 다 매우 강력함!

---

## 2. HTTPS 하이브리드 암호화 방식

### 왜 하이브리드를 사용하나?

**각 방식의 문제점**:

- 대칭키: 키 공유 어려움 (키 분배 문제)
- 비대칭키: 너무 느림

**해결책**: 둘을 섞어서 사용!

### HTTPS 동작 과정

```
1단계: Handshake (비대칭키 사용)
┌─────────────────────────────────────┐
│ 목적: 대칭키를 안전하게 교환       │
├─────────────────────────────────────┤
│ 1. 클라: 대칭키(세션키) 생성       │
│ 2. 서버의 공개키로 대칭키 암호화   │
│ 3. 암호화된 대칭키 전송            │
│ 4. 서버: 개인키로 복호화           │
│ 5. 양쪽 모두 같은 대칭키 보유 ✅   │
└─────────────────────────────────────┘

2단계: 데이터 통신 (대칭키 사용)
┌─────────────────────────────────────┐
│ 목적: 빠른 속도로 대용량 데이터    │
├─────────────────────────────────────┤
│ - 모든 HTTP 통신은 대칭키로 암호화 │
│ - Netflix 영화, 웹페이지 등        │
└─────────────────────────────────────┘
```

---

## 3. TLS Handshake 상세 과정

### 단계별 흐름

```
1. Client Hello
   클라 → 서버: "내가 지원하는 암호 알고리즘 목록이야"

2. Server Hello + Certificate
   서버 → 클라: "이 암호 쓰자. 그리고 내 인증서(공개키 포함)야"

3. 인증서 검증 & 세션키 교환
   클라: - 인증서 검증 (진짜 서버 맞나?)
         - 랜덤한 세션키 생성
         - 서버의 공개키로 세션키 암호화
         - 암호화된 세션키 전송

4. 복호화
   서버: 자신의 개인키로 복호화 → 세션키 획득

5. 암호화 통신 시작 ✅
   이제 양쪽 모두 같은 세션키를 가짐
```

### 인증서와 공개키

**인증서 = 서버의 신분증**

```
┌─────────────────────┐
│     인증서          │
├─────────────────────┤
│ 도메인: google.com  │
│ 발급: Let's Encrypt │
│ 유효기간: 2025-12-31│
│ 공개키: AAABBB...   │ ← 이미 여기 포함!
└─────────────────────┘
```

**중요**:

- 공개키는 평문(암호화되지 않음)으로 전송됨
- 누구나 볼 수 있음 (그래서 "공개"키)
- 하지만 공개키로 암호화한 데이터는 서버의 개인키로만 복호화 가능!

---

## 4. 네트워크 계층과 프로토콜

### OSI 7계층 (HTTPS 관점)

```
응용 계층:    HTTP / HTTPS
              ↓
보안 계층:    TLS/SSL ← HTTPS일 때만
              ↓
전송 계층:    TCP
              ↓
네트워크 계층: IP
```

### HTTPS 연결 순서 (아래에서 위로!)

```
1. TCP 3-way Handshake (전송 계층)
   ┌─────────────────────┐
   │ SYN →              │
   │      ← SYN-ACK     │
   │ ACK →              │
   │ ✅ TCP 연결 완료    │
   └─────────────────────┘

2. TLS Handshake (보안 계층)
   ┌─────────────────────┐
   │ 암호 협상           │
   │ 인증서 교환         │
   │ 세션키 교환         │
   │ ✅ 보안 연결 완료   │
   └─────────────────────┘

3. HTTP 통신 (응용 계층)
   ┌─────────────────────┐
   │ GET, POST 요청      │
   │ ✅ 데이터 송수신    │
   └─────────────────────┘
```

### TCP 연결 생성/종료

**3-way Handshake (연결 시작)**

```
클라 → 서버: SYN
서버 → 클라: SYN-ACK
클라 → 서버: ACK
```

**4-way Handshake (연결 종료)**

```
클라 → 서버: FIN
서버 → 클라: ACK
서버 → 클라: FIN  ← 서버도 보낼 데이터 다 보낸 후
클라 → 서버: ACK
```

**왜 종료는 4-way?**
서버가 아직 보낼 데이터가 남아있을 수 있어서 ACK와 FIN을 분리함

---

## 5. TCP vs UDP

### 특성 비교

| 특성      | TCP           | UDP     |
| --------- | ------------- | ------- |
| 연결      | 연결 지향     | 비연결  |
| 신뢰성    | 보장 (재전송) | 미보장  |
| 순서      | 보장          | 미보장  |
| 속도      | 느림          | 빠름    |
| 헤더 크기 | 20바이트      | 8바이트 |

### 사용 사례

**TCP 사용**

- HTTP/HTTPS (웹)
- 파일 전송 (FTP)
- 이메일 (SMTP)
- SSH

**UDP 사용**

- 실시간 화상/음성 통화 (Zoom, Discord, WebRTC)
- 온라인 게임 (FPS 게임)
- 라이브 스트리밍 (Twitch)
- DNS (도메인 조회)
- IoT 센서

### 비유

```
TCP = 등기우편 📬
- 확실하게 도착
- 서명 확인
- 느림

UDP = 일반우편 📪
- 빠름
- 가끔 분실 가능
- 중요하지 않은 내용
```

### HTTP/3의 등장

```
HTTP/1.1, HTTP/2: TCP 기반
HTTP/3 (QUIC): UDP 기반!

왜?
- TCP의 HOL Blocking 문제 해결
- 더 빠른 연결
- 하지만 UDP 위에 신뢰성 직접 구현
```

---

## 6. Spring MVC vs WebFlux

### 둘 다 TCP 기반!

**❌ 오해**: MVC = TCP, WebFlux = UDP
**✅ 실제**: 둘 다 TCP 사용, 차이는 처리 방식

### 처리 방식 차이

**Spring MVC (Blocking/동기)**

```
요청 → 스레드 1개 할당
       ↓
     DB 조회 (대기중... 🛑)
       ↓
     응답 반환

스레드가 작업 끝날 때까지 블로킹
```

**Spring WebFlux (Non-blocking/비동기)**

```
요청 → 스레드 잠깐 사용
       ↓
     DB 조회 시작 (스레드 반납 🔄)
       ↓
     다른 요청 처리...
       ↓
     DB 응답 오면 다시 처리
```

### 비유

**MVC = 동기식 전화 📞**

- 상대방 응답 올 때까지 기다림
- 그동안 다른 일 못 함

**WebFlux = 비동기식 메신저 💬**

- 메시지 보내고 다른 일 함
- 답장 오면 알림으로 확인

### 언제 뭘 쓰나?

**MVC 추천**

- 일반적인 웹 애플리케이션
- CRUD 중심
- 이해하기 쉬움
- 대부분의 경우 충분함 ✅

**WebFlux 추천**

- 많은 동시 접속 (채팅, 스트리밍)
- I/O 대기 시간이 긴 경우
- MSA 환경

---

## 7. S3/Object Storage

### Content-Length

**거의 항상 제공됨!**

```http
GET /video.mp4 HTTP/1.1

HTTP/1.1 200 OK
Content-Length: 104857600  ← 전체 파일 크기
```

S3는 객체 크기를 알고 있기 때문에 항상 제공

### Range 요청

**대용량 파일 부분 다운로드**

```http
GET /video.mp4 HTTP/1.1
Range: bytes=0-1023  ← 첫 1KB만

HTTP/1.1 206 Partial Content
Content-Length: 1024        ← 이번 조각 크기
Content-Range: bytes 0-1023/104857600
                           ↑ 전체 크기
```

**사용 사례**

- 동영상 스트리밍 (탐색 시)
- 다운로드 재개
- 병렬 다운로드

---

## 8. 핵심 개념 정리

### 용어 구분

| 용어       | 종류     | 설명                        |
| ---------- | -------- | --------------------------- |
| **공개키** | 비대칭키 | 암호화용, 누구나 볼 수 있음 |
| **개인키** | 비대칭키 | 복호화용, 소유자만 가짐     |
| **대칭키** | 대칭키   | 암호화=복호화, 양쪽이 공유  |
| **세션키** | 대칭키   | = 대칭키 (사용 기간 강조)   |
| **비밀키** | 대칭키   | = 대칭키 (보안 특성 강조)   |
| **공유키** | 대칭키   | = 대칭키 (소유 관계 강조)   |

### 암호화 과정 요약

```javascript
// 클라이언트
const sessionKey = generateRandomKey(); // 1. 세션키 생성
const encrypted = RSA.encrypt(
  sessionKey, // 암호화 대상
  serverPublicKey // 암호화 도구 (비대칭키 사용!)
);
send(encrypted);

// 서버
const encrypted = receive();
const sessionKey = RSA.decrypt(
  encrypted,
  serverPrivateKey // 복호화 도구
);
// 이제 양쪽 모두 같은 세션키 보유!
```

### 전체 흐름 한눈에

```
HTTPS 연결 전체 과정:

1. DNS 조회 (UDP)
   ↓
2. TCP 3-way Handshake (전송 계층)
   ↓
3. TLS Handshake (보안 계층, 비대칭키)
   - 인증서 교환
   - 세션키 안전하게 교환
   ↓
4. HTTP 통신 (응용 계층, 대칭키)
   - 모든 데이터 암호화
   ↓
5. TCP 4-way Handshake (연결 종료)
```

---

## 9. 프론트엔드 개발자가 알아야 할 것

### 실무 연관성

```javascript
// HTTPS 자동 처리
fetch("https://api.example.com/data");
// 브라우저가 자동으로:
// 1. TCP 연결
// 2. TLS Handshake
// 3. 세션키로 암호화 통신

// Content-Type (15장 내용)
fetch("/api", {
  headers: {
    "Content-Type": "application/json",
  },
});

// Range 요청 (동영상)
fetch("https://cdn.com/video.mp4", {
  headers: {
    Range: "bytes=0-1024",
  },
});

// WebRTC (UDP 기반)
const pc = new RTCPeerConnection();
// 화상통화는 UDP로!
```

### Mixed Content 이슈

```html
<!-- HTTPS 페이지에서 -->
<img src="http://..." /> ❌ 브라우저가 차단 <img src="https://..." /> ✅ 허용
```

### 브라우저 개발자 도구

```
Network 탭에서 확인:
- Protocol: h2 (HTTP/2), h3 (HTTP/3)
- 자물쇠 아이콘: TLS 인증서 정보
- Timing: TCP, TLS 시간 확인 가능
```

---

## 10. 학습 포인트

### 가장 중요한 개념

1. **하이브리드 암호화의 이유**

   - 비대칭키: 키 분배 문제 해결
   - 대칭키: 빠른 속도
   - 둘의 장점을 결합!

2. **계층 구조 이해**

   - 아래에서 위로: TCP → TLS → HTTP
   - 각 계층이 순차적으로 준비

3. **대칭키 ≠ 약한 암호화**

   - 보안 강도는 비대칭키와 동일
   - 단지 키 공유가 문제였을 뿐

4. **용어의 다양성**
   - 세션키 = 대칭키 = 비밀키 = 공유키
   - 문맥에 따라 다르게 부름

### 다음 학습 추천

- TLS 1.3의 개선사항
- Certificate Pinning
- Forward Secrecy
- HTTP/3와 QUIC 프로토콜
- CSP (Content Security Policy)

---

## 참고: 비유 모음

### 우편 시스템

- **공개키** = 우체통 (누구나 편지 넣을 수 있음)
- **개인키** = 우체통 열쇠 (주인만 꺼낼 수 있음)
- **대칭키** = 금화 (전달할 내용물)

### 식당

- **MVC** = 웨이터 1명이 손님 1팀 전담
- **WebFlux** = 웨이터 1명이 여러 손님 동시 처리

### 우편

- **TCP** = 등기우편 (확실하지만 느림)
- **UDP** = 일반우편 (빠르지만 가끔 분실)

---

**학습 날짜**: 2025년 11월 2일
**주요 교재**: HTTP 네트워크 완벽 가이드 14, 15장
